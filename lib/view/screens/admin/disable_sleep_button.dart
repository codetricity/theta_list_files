import 'package:flutter/material.dart';

class DisableSleepButton extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return TextButton(onPressed: () {}, child: Text('disable sleep'));
  }
}
